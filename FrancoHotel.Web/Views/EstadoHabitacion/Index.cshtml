@model IEnumerable<FrancoHotel.Application.Dtos.EstadoHabitacionDtos.UpdateEstadoHabitacionDto>

@{
    ViewData["Title"] = "Index";
}

<h1>Lista de Estados de Habitación</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.IdEstadoHabitacion)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Descripcion)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Estado)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Fecha)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Usuario)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.IdEstadoHabitacion)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Descripcion)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Estado)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Fecha)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Usuario)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.IdEstadoHabitacion }) |
                @Html.ActionLink("Details", "Details", new { id = item.IdEstadoHabitacion }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.IdEstadoHabitacion })
            </td>
        </tr>
}
    </tbody>
</table>
